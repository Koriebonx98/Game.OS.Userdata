# ── Required ───────────────────────────────────────────────────────────────────

# GitHub Personal Access Token with "repo" scope (read/write to private repos)
# Generate at: https://github.com/settings/tokens → "Generate new token (classic)"
# Required scopes: repo (Full control of private repositories)
GITHUB_TOKEN=your_github_personal_access_token_here

# Private repository where account data will be stored (JSON files per user)
# Create this repo at https://github.com/new → set it to Private
REPO_OWNER=YourGitHubUsername
REPO_NAME=Game.OS.Private.Data

# Secret key used to sign Game OS API tokens (HMAC-SHA256).
# Generate a strong random string – e.g.: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
# Keep this private and NEVER commit the real value to source control.
TOKEN_HMAC_SECRET=change_me_to_a_long_random_secret

# ── Public API Key ──────────────────────────────────────────────────────────────
# A single shared key for C# game launchers / apps that need to read user data
# (profiles, game libraries, achievements) without authenticating as a specific user.
# Any Bearer token matching this value will be granted read access to the
# /api/users/:username  /api/users/:username/games  /api/users/:username/achievements
# endpoints.  Keep it private to your app – do NOT commit the real value.
# Generate: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
PUBLIC_API_KEY=change_me_to_a_long_random_secret

# ── Optional ───────────────────────────────────────────────────────────────────

# Port the server listens on (default: 3000)
# Railway / Render set this automatically
PORT=3000

# Your frontend URL (used for CORS).  Leave blank to allow all origins.
# Example: https://koriebonx98.github.io
ALLOWED_ORIGIN=

# Secret key required for the /api/reset-all-accounts endpoint.
# Set to a long random string and keep it private.
ADMIN_KEY=change_me_to_a_long_random_secret
